{% extends 'base.html.twig' %}

{% block title %}Your account{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>{% trans %}Manage your account{% endtrans %} {{ app.user.firstName }}</h1>
    <ul>
        <li><a href="{{ path('user_show', {'id': app.user.id}) }}">Profile info</a></code></li>
        <li><a href="{{ path('user_show', {'id': app.user.id}) }}">Your documents</a></code></li>
    </ul>
    {% if is_granted('ROLE_ADMIN') %}
    	<h1>Administration</h1>
        <ul>
            <li><a href="{{ path('user_index') }}">Manage users</a></code></li>
            <li><a href="{{ path('user_index') }}">Manage documents</a></code></li>
        </ul>
    {% endif %}
</div>
{% endblock %}
